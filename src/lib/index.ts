/**
 * For reusable, non-ui logic functions
 *
 * Export all functions/modules from subdirectories within this,
 * and then re-export them from here, then import them with the '@/lib/' prefix
 *
 * Example,
 * addTwoNumbers.ts
 * export function addTwoNums(a:number, b:number) {
 *      return a + b
 * }
 *
 * Reexporting from here,
 * export * from "./addTwoNumbers";
 *
 * Then import as
 * import {addTwoNumbers} from "@/lib"
 */
